
function mostrar()
{
    var ancho;
    var largo;
    var perimetro;

    ancho = prompt("ancho");
    largo = prompt("largo");

    perimetro = (largo * 2) + (ancho * 2);

    alert("El perimetro es " + perimetro);

}
